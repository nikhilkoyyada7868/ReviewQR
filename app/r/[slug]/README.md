# Customer route boundary

Stage 3 owns the `/r/[slug]` server page and its client interaction component.
It may import contracts/application services, but it must not access D1 or an AI
provider directly. Unknown and inactive restaurants share a non-leaking state.
